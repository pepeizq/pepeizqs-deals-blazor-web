@using Microsoft.VisualBasic

<HeadContent>
	<PageTitle>@titulo</PageTitle>

	<meta name="og:title" Content="@titulo" />
	<meta name="description" content="@descripcion" />
	<meta name="og:description" content="@descripcion" />
	<meta name="og:url" content="@enlace" />
	<meta name="og:image" content="@imagen" />
	<meta name="og:site_name" content="pepe's deals" />
	<meta name="og:type" content="website" />
	<meta name="robots" content="noimageindex" />
	<meta name="keywords" content="@keywords2" />

	@if (tipo == Tipo.Bundles)
	{
		if (string.IsNullOrEmpty(contenidoJsonBundles) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonBundles)
			</script>
		}
	}
	else if (tipo == Tipo.Bundle)
	{
		if (string.IsNullOrEmpty(contenidoJsonBundle) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonBundle)
			</script>
		}
	}
	else if (tipo == Tipo.Curator)
	{
		if (string.IsNullOrEmpty(contenidoJsonCurator) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonCurator)
			</script>
		}
	}
	else if (tipo == Tipo.Gratis)
	{
		if (string.IsNullOrEmpty(contenidoJsonGratis) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonGratis)
			</script>
		}
	}
	else if (tipo == Tipo.Juego)
	{
		if (string.IsNullOrEmpty(mayorEdadJuego) == false)
		{
			if (mayorEdadJuego.ToLower() == "true")
			{
				<meta name="rating" content="adult">
			}
		}

		string nombreJson = nombreJuego.Replace(Strings.ChrW(34).ToString(), null);
		string descripcionJson = descripcion.Replace(Strings.ChrW(34).ToString(), null);

		if (string.IsNullOrEmpty(imagen) == false && estrellasJsonJuego > 0 && string.IsNullOrEmpty(cantidadJsonJuego) == false)
		{
			if (string.IsNullOrEmpty(precioBajoJsonJuego) == false && string.IsNullOrEmpty(precioAltoJsonJuego) == false)
			{
				<script type="application/ld+json">
					{
						"@@context": "https://schema.org/",
						"@@type": "Product",
						"name": "@nombreJson",
						"description": "@descripcionJson",
						"url": "@enlace",
						"sku": "@idJuego.ToString()",
						"image": {
							"@@type": "ImageObject",
							"url": "@imagen",
							"contentUrl": "@imagen",
							"name": "@nombreJson",
							"caption": "@nombreJson"
						},
						"productionDate": "@fechaJsonJuego",
						"offers": {
							"@@type": "AggregateOffer",
							"availability": "InStock",
							"priceCurrency": "EUR",
							"price": @precioBajoJsonJuego,
							"url": "@enlace",
							"offerCount": @preciosCantidadJsonJuego,
							"lowPrice": @precioBajoJsonJuego,
							"highPrice": @precioAltoJsonJuego
						},
						"aggregateRating": {
							"@@type": "AggregateRating",
							"ratingValue": @estrellasJsonJuego.ToString(),
							"ratingCount": @cantidadJsonJuego
						}
					}
				</script>
			}
			else
			{
				<script type="application/ld+json">
					{
						"@@context": "https://schema.org/",
						"@@type": "Product",
						"name": "@nombreJson",
						"description": "@descripcionJson",
						"url": "@enlace",
						"sku": "@idJuego.ToString()",
						"image": {
							"@@type": "ImageObject",
							"url": "@imagen",
							"contentUrl": "@imagen",
							"name": "@nombreJson",
							"caption": "@nombreJson"
						},
						"aggregateRating": {
							"@@type": "AggregateRating",
							"ratingValue": @estrellasJsonJuego.ToString(),
							"ratingCount": @cantidadJsonJuego
						}
					}
				</script>
			}
		}

	}
	else if (tipo == Tipo.Noticia)
	{
		if (string.IsNullOrEmpty(contenidoJsonNoticia) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonNoticia)
			</script>
		}
	}
	else if (tipo == Tipo.Portada)
	{
		<script type="application/ld+json">
			{
			  "@@context": "https://schema.org",
			  "@@type": "BreadcrumbList",
			  "itemListElement": [{
				"@@type": "ListItem",
				"position": 1,
				"name": "Bundles",
				"item": "https://@dominio/bundles"
			  },{
				"@@type": "ListItem",
				"position": 2,
				"name": "Historial Lows",
				"item": "https://@dominio/historical-lows"
			  },{
				"@@type": "ListItem",
				"position": 3,
				"name": "Free",
				"item": "https://@dominio/free"
			  },{
				"@@type": "ListItem",
				"position": 4,
				"name": "Subscriptions",
				"item": "https://@dominio/subscriptions"
			  },{
				"@@type": "ListItem",
				"position": 5,
				"name": "Patreon",
				"item": "https://@dominio/patreon"
			  },{
				"@@type": "ListItem",
				"position": 6,
				"name": "Compare",
				"item": "https://@dominio/compare"
			  },{
				"@@type": "ListItem",
				"position": 7,
				"name": "API",
				"item": "https://@dominio/api"
			  },{
				"@@type": "ListItem",
				"position": 8,
				"name": "Last Added",
				"item": "https://@dominio/last-added"
			  }]
			}
		</script>

		if (string.IsNullOrEmpty(contenidoJsonPortada) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonPortada)
			</script>
		}
	}
	else if (tipo == Tipo.Suscripciones)
	{
		if (string.IsNullOrEmpty(contenidoJsonSuscripciones) == false)
		{
			<script type="application/ld+json">
				@((MarkupString)contenidoJsonSuscripciones)
			</script>
		}
	}
	else if (tipo == Tipo.UltimasNoticias)
	{
		<script type="application/ld+json">
			{
				"@@context": "https://schema.org",
				"@@type": "BreadcrumbList",
				"itemListElement": [
				@((MarkupString)contenidoJsonUltimasNoticias)
				]
			}
		</script>
	}

	@if (tipo != Tipo.Noticia)
	{
		<link rel="canonical" href="@enlace" />
	}
	else
	{
		<link rel="canonical" href="@enlaceEnNoticia"/>
	}
	
	<link title="pepe's deals • @Herramientas.Idiomas.BuscarTexto(idioma, "Title", "LastNews")" rel="alternate" type="application/rss+xml" href="https://@dominio/rss-en.xml" />

	<meta name="twitter:site" content="@@pepeizqdeals" />
	<meta name="twitter:domain" content="pepeizqdeals.com" />
	<meta name="twitter:card" content="summary_large_image" />
</HeadContent>

@code {

	#nullable disable

	[Parameter]
	public string idioma { get; set; }

	[Parameter]
	public string dominio { get; set; }

	[Parameter]
	public Tipo tipo { get; set; }

	[Parameter]
	public string enlace { get; set; }

	[Parameter]
	public string keywords { get; set; }

	#region Parametros Bundle

	[Parameter]
	public string tituloBundle { get; set; }

	[Parameter]
	public string descripcionBundle { get; set; }

	[Parameter]
	public string enlaceBundle { get; set; }

	[Parameter]
	public string imagenBundle { get; set; }

	[Parameter]
	public string contenidoJsonBundle { get; set; }

	#endregion

	#region Parametros Bundles

	[Parameter]
	public string tituloBundles { get; set; }

	[Parameter]
	public string contenidoJsonBundles { get; set; }

	#endregion

	#region Parametros Coste

	[Parameter]
	public string descripcionCoste { get; set; }

	#endregion

	#region Parametros Curator

	[Parameter]
	public string tituloCurator { get; set; }

	[Parameter]
	public string descripcionCurator { get; set; }

	[Parameter]
	public string enlaceCurator { get; set; }

	[Parameter]
	public string imagenCurator { get; set; }

	[Parameter]
	public string contenidoJsonCurator { get; set; }

	#endregion

	#region Parametros Juego

	[Parameter]
	public int idJuego { get; set; }

	[Parameter]
	public string nombreJuego { get; set; }

	[Parameter]
	public string descripcionJuego { get; set; }

	[Parameter]
	public string imagenJuego { get; set; }

	[Parameter]
	public List<Secciones.Juego.DRMEnseñar> drmsEnseñarJuego { get; set; }

	[Parameter]
	public bool gamePassJuego { get; set; }

	[Parameter]
	public string mayorEdadJuego { get; set; }

	[Parameter]
	public List<string> categoriasJuego { get; set; }

	[Parameter]
	public decimal estrellasJsonJuego { get; set; }

	[Parameter]
	public string cantidadJsonJuego { get; set; }

	[Parameter]
	public string precioBajoJsonJuego { get; set; }

	[Parameter]
	public string precioAltoJsonJuego { get; set; }

	[Parameter]
	public int? preciosCantidadJsonJuego { get; set; } = 0;

	[Parameter]
	public string fechaJsonJuego { get; set; }

	#endregion

	#region Parametros Filtro

	[Parameter]
	public string idsFiltro { get; set; }

	#endregion

	#region Parametros Gratis

	[Parameter]
	public string tituloGratis { get; set; }

	[Parameter]
	public string contenidoJsonGratis { get; set; }

	#endregion

	#region Parametros Noticia

	[Parameter]
	public string tituloNoticia { get; set; }

	[Parameter]
	public string descripcionNoticia { get; set; }

	[Parameter]
	public string enlaceEnNoticia { get; set; }

	[Parameter]
	public string enlaceEsNoticia { get; set; }

	[Parameter]
	public string imagenNoticia { get; set; }

	[Parameter]
	public string contenidoJsonNoticia { get; set; }

	#endregion

	#region Parametros Portada

	[Parameter]
	public string contenidoJsonPortada { get; set; }

	#endregion

	#region Parametros Suscripciones

	[Parameter]
	public string tituloAlternativoSuscripciones { get; set; }

	[Parameter]
	public string enlaceAlternativoSuscripciones { get; set; }

	[Parameter]
	public string contenidoJsonSuscripciones { get; set; }

	#endregion

	#region Parametros Ultimas Noticias

	[Parameter]
	public string contenidoJsonUltimasNoticias { get; set; }

	#endregion

	[PersistentState] public string titulo { get; set; }
	[PersistentState] public string descripcion { get; set; }
	[PersistentState] public string imagen { get; set; }
	[PersistentState] public string keywords2 { get; set; }
	[PersistentState] public TituloKeywords tituloKeywords { get; set; }

	public string keywordsVacias = "game deals, digital distribution, Steam, pc game, price tracker, price comparison, Epic Games, PC Game Pass, bundles, free";

	public enum Tipo
	{
		Otros,
		Api,
		Bundle,
		Bundles,
		Comparador,
		Contacto,
		Coste,
		Curator,
		Deseados,
		Filtro,
		Gratis,
		Juego,
		Minimos,
		Noticia,
		Patreon,
		Portada,
		Privacidad,
		RSS,
		Suscripciones,
		UltimasNoticias,
		UltimosAñadidos
	}

	protected override void OnInitialized()
	{
		if (tipo == Tipo.Api)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Api");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Api");
			enlace ??= "https://" + dominio + "/api";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Bundle)
		{
			titulo ??= tituloBundle;
			descripcion ??= descripcionBundle;
			enlace ??= enlaceBundle;
			imagen ??= imagenBundle;
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Bundles)
		{
			titulo ??= tituloBundles;
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Bundle");
			enlace ??= "https://" + dominio + "/bundles/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Comparador)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Compare");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String20", "Compare");
			enlace ??= "https://" + dominio + "/compare/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Contacto)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Contact");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Contact");
			enlace ??= "https://" + dominio + "/contact/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Coste)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "CostWeb");
			descripcion ??= descripcionCoste;
			enlace ??= "https://" + dominio + "/cost-web";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Curator)
		{
			titulo ??= tituloCurator;
			descripcion ??= descripcionCurator;
			enlace ??= enlaceCurator;
			imagen ??= imagenCurator;
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Deseados)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Wishlist");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Wishlist");
			enlace ??= "https://" + dominio + "/wishlist/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Filtro)
		{
			tituloKeywords ??= FiltroTituloKeywords(idsFiltro);

			titulo ??= tituloKeywords.Titulo;
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Filter");
			enlace ??= "https://" + dominio + "/filter/" + idsFiltro + "/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= tituloKeywords.Keywords;
		}
		else if (tipo == Tipo.Gratis)
		{
			titulo ??= tituloGratis;
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String11", "Free");
			enlace ??= "https://" + dominio + "/free/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Juego)
		{
			titulo ??= JuegoTitulo(nombreJuego, drmsEnseñarJuego, gamePassJuego);

			if (string.IsNullOrEmpty(descripcionJuego) == false)
			{
				descripcion ??= descripcionJuego;
			}
			else
			{
				descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String104", "Game");
			}
			
			enlace ??= "https://" + dominio + "/game/" + idJuego.ToString() + "/" + Herramientas.EnlaceAdaptador.Nombre(nombreJuego) + "/";
			imagen ??= imagenJuego;
			keywords2 ??= JuegoKeywords(categoriasJuego);
		}
		else if (tipo == Tipo.Minimos)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "HistoricalLows") ;
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String22", "HistoricalLows");
			enlace ??= "https://" + dominio + "/historical-lows/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Noticia)
		{
			titulo ??= tituloNoticia;
			descripcion ??= descripcionNoticia;

			if (idioma == "es")
			{
				enlace ??= enlaceEsNoticia;
			}
			else
			{
				enlace ??= enlaceEnNoticia;
			}

			imagen ??= imagenNoticia;
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.Patreon)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Patreon");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Patreon");
			enlace ??= "https://" + dominio + "/patreon";
			imagen ??= "https://" + dominio + "/logo/logoentradapatreon.webp";
			keywords ??= "patreon, " + keywordsVacias;
		}
		else if (tipo == Tipo.Portada)
		{
			titulo ??= "pepe's deals • " + Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Index");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Index");
			enlace ??= "https://" + dominio + "/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Privacidad)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Privacy");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Privacy");
			enlace ??= "https://" + dominio + "/privacy/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.RSS)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "RSS");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "RSS");
			enlace ??= "https://" + dominio + "/rss";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywordsVacias;
		}
		else if (tipo == Tipo.Suscripciones)
		{
			if (string.IsNullOrEmpty(tituloAlternativoSuscripciones) == false)
			{
				titulo = tituloAlternativoSuscripciones;
			}
			else
			{
				titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Subscriptions");
			}

			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "Subtitle", "Subscriptions");

			if (string.IsNullOrEmpty(enlaceAlternativoSuscripciones) == false)
			{
				enlace = "https://" + dominio + "/subscriptions/" + enlaceAlternativoSuscripciones + "/";
			}
			else
			{
				enlace ??= "https://" + dominio + "/subscriptions/";
			}

			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.UltimasNoticias)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "LastNews") ;
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String4", "LastNews");
			enlace ??= "https://" + dominio + "/last-news/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}
		else if (tipo == Tipo.UltimosAñadidos)
		{
			titulo ??= Herramientas.Idiomas.BuscarTexto(idioma, "Title", "LastAdded");
			descripcion ??= Herramientas.Idiomas.BuscarTexto(idioma, "String1", "LastAdded");
			enlace ??= "https://" + dominio + "/last-added/";
			imagen ??= "https://" + dominio + "/logo/logoentrada.webp";
			keywords2 ??= keywords;
		}

		#region Encajar Titulo y Descripcion

		if (string.IsNullOrEmpty(titulo) == false)
		{
			titulo = titulo.Replace(Strings.ChrW(34).ToString(), null);
		}

		if (titulo.Length > 56)
		{
			titulo = titulo.Substring(0, 56) + " ...";
		}
		else
		{
			if (titulo.Length < 39 && tipo != Tipo.Portada)
			{
				if (titulo.Contains(" • pepe's deals") == false)
				{
					titulo = titulo + " • pepe's deals";
				}
			}
		}

		if (string.IsNullOrEmpty(descripcion) == false)
		{
			descripcion = descripcion.Replace(Strings.ChrW(34).ToString(), null);

			if (descripcion.Length > 146)
			{
				descripcion = descripcion.Substring(0, 146) + " ...";
			}
		}

		

		#endregion
	}

	private string JuegoTitulo(string nombre, List<Secciones.Juego.DRMEnseñar> drmsEnseñar, bool gamePass)
	{
		string tituloJuego = nombre;
		string tituloDRMs = string.Empty;

		foreach (var drm in drmsEnseñar)
		{
			if (string.IsNullOrEmpty(drm.Mensaje) == false)
			{
				if (drm.DRM == Juegos.JuegoDRM.Steam || drm.DRM == Juegos.JuegoDRM.GOG)
				{
					bool añadir = true;

					if (string.IsNullOrEmpty(tituloDRMs) == false)
					{
						if (tituloDRMs.Contains(Juegos.JuegoDRM2.DevolverDRM(drm.DRM)) == true)
						{
							añadir = false;
						}
					}

					if (añadir == true)
					{
						if (string.IsNullOrEmpty(tituloDRMs) == false)
						{
							tituloDRMs = tituloDRMs + ", ";
						}

						tituloDRMs = tituloDRMs + Juegos.JuegoDRM2.DevolverDRM(drm.DRM);
					}
				}
			}
		}

		if (gamePass == true)
		{
			if (string.IsNullOrEmpty(tituloDRMs) == false)
			{
				tituloDRMs = tituloDRMs + ", ";
			}

			tituloDRMs = tituloDRMs + "PC Game Pass";
		}

		if (string.IsNullOrEmpty(tituloDRMs) == false)
		{
			tituloDRMs = " (" + tituloDRMs + ")";
			tituloJuego = tituloJuego + tituloDRMs;
		}

		return tituloJuego;
	}

	private string JuegoKeywords(List<string> categorias)
	{
		string keywords = null;

		if (categorias?.Count > 0)
		{
			foreach (var categoria in categorias)
			{
				string cadenaCategoria = Herramientas.Idiomas.BuscarTexto("en", categoria, "Categories");

				if (string.IsNullOrEmpty(keywords) == true)
				{
					keywords = cadenaCategoria;
				}
				else
				{
					keywords = keywords + ", " + cadenaCategoria;
				}
			}
		}

		if (string.IsNullOrEmpty(keywords) == false)
		{
			keywords = keywords + ", ";
		}

		keywords = keywords + keywordsVacias;

		return keywords;
	}

	public class TituloKeywords
	{
		public string Titulo { get; set; }
		public string Keywords { get; set; }
	}

	private TituloKeywords FiltroTituloKeywords(string idsFiltro)
	{
		TituloKeywords tituloKeywords = new TituloKeywords();
		tituloKeywords.Titulo = Herramientas.Idiomas.BuscarTexto(idioma, "Title", "Filter");
		tituloKeywords.Keywords = "Filter";

		if (string.IsNullOrEmpty(idsFiltro) == false)
		{
			string[] ids2 = idsFiltro.Split(',');

			if (ids2.Length > 0)
			{
				foreach (string id in ids2)
				{
					if (string.IsNullOrEmpty(id) == false)
					{
						if (id.ToLower().Contains("t") == true)
						{
							string cadena = id.Replace("t", null);

							if (tituloKeywords.Titulo.Contains("•") == false)
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + " • " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Tags");
							}
							else
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + ", " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Tags");
							}

							tituloKeywords.Keywords = tituloKeywords.Keywords + ", " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Tags");
						}

						if (id.ToLower().Contains("c") == true)
						{
							string cadena = id.Replace("c", null);

							if (tituloKeywords.Titulo.Contains("•") == false)
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + " • " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Categories");
							}
							else
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + ", " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Categories");
							}

							tituloKeywords.Keywords = tituloKeywords.Keywords + ", " + Herramientas.Idiomas.BuscarTexto(idioma, cadena, "Categories");
						}

						if (id.ToLower().Contains("s") == true)
						{
							string cadena = id.Replace("s", null);

							if (tituloKeywords.Titulo.Contains("•") == false)
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + " • " + Herramientas.Idiomas.BuscarTexto(idioma, "System" + cadena, "Filter");
							}
							else
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "System" + cadena, "Filter");
							}

							tituloKeywords.Keywords = tituloKeywords.Keywords + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "System" + cadena, "Filter");
						}

						if (id.ToLower().Contains("i") == true)
						{
							string cadena = id.Replace("i", null);

							if (tituloKeywords.Titulo.Contains("•") == false)
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + " • " + Herramientas.Idiomas.BuscarTexto(idioma, "Type" + cadena, "Filter");
							}
							else
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "Type" + cadena, "Filter");
							}

							tituloKeywords.Keywords = tituloKeywords.Keywords + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "Type" + cadena, "Filter");
						}

						if (id.ToLower().Contains("d") == true)
						{
							string cadena = id.Replace("d", null);

							if (tituloKeywords.Titulo.Contains("•") == false)
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + " • " + Herramientas.Idiomas.BuscarTexto(idioma, "Deck" + cadena, "Filter");
							}
							else
							{
								tituloKeywords.Titulo = tituloKeywords.Titulo + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "Deck" + cadena, "Filter");
							}

							tituloKeywords.Keywords = tituloKeywords.Keywords + ", " + Herramientas.Idiomas.BuscarTexto(idioma, "Deck" + cadena, "Filter");
						}
					}
				}
			}
		}

		return tituloKeywords;
	}

}
